[package]
name = "keybind-flash"
version = "0.1.0"
edition = "2021"

[dependencies]
gtk = { version = "0.15", features = ["v3_22"] }
gdk = "0.15"
evdev = "0.12"
libc = "0.2"
glib = "0.21.5"
pulldown-cmark = "0.12"

[profile.dev]
debug = true
split-debuginfo = "packed"
